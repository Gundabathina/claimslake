# Gold-layer (dbt) dependencies. Kept separate from the root requirements.txt
# so the Bronze/Silver runtime stays lightweight.
#   pip install -r dbt/requirements-dbt.txt
dbt-core>=1.7,<2.0
dbt-duckdb>=1.7,<2.0
