# scripts/

Standalone helper scripts that support the project but aren't part of the orchestrated pipeline itself.

- `generate_synthetic_data.py` — generates the synthetic members/providers/diagnoses/claims datasets (Milestone 1)
- Local setup/teardown helpers for the Docker stack, MinIO bucket creation, and warehouse database initialization
