# sql/

Hand-written SQL that sits alongside dbt: warehouse DDL, staging table definitions, and analytical queries used for ad hoc analysis and README examples.

Files (added incrementally): `ddl/` for table creation scripts, `analytics/` for example business-question queries (e.g. denial rate by provider, cost by diagnosis, claims processing time trends).
